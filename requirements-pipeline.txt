# Minimale Abhängigkeiten für die WM-2026-Pipeline im Docker-Container.
# Nur was prepare_data / train / visualize / predict / notify wirklich brauchen.
# Versionen aus requirements.txt übernommen → reproduzierbare Builds.

numpy==2.4.5
pandas==3.0.3
PyYAML==6.0.3
joblib==1.5.3
xgboost==3.2.0
scikit-learn==1.8.0
matplotlib==3.10.9
seaborn==0.13.2
requests==2.33.1
python-dotenv==1.2.2
